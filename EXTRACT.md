# Extracting the source

The full Twin Engine monorepo source is stored in `twin-engine-source.tar.gz`.

```bash
mkdir -p source
tar -xzf twin-engine-source.tar.gz -C source
```

The archive includes a repository manifest with SHA-256 hashes for every included file.